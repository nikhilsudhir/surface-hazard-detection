# Core training framework — includes YOLO model definitions, training loop, and metrics
ultralytics>=8.2.0

# Deep learning backend — CPU-only by default;
# GPU users should install the CUDA build separately
#   (see README for GPU install instructions)
torch>=2.0.0
torchvision>=0.15.0

# Scientific computing and image handling
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Metrics, plotting, and results export
matplotlib>=3.7.0
pandas>=2.0.0
scipy>=1.11.0

# Progress bars and experiment logging
pyyaml>=6.0
